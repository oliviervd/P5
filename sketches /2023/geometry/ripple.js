// p5 sketch that generates ripple when clicked.
// starting at the position where it is clicked. 

const black = 0;
const white = "f1f1f1"
let r = 0;

function setup() {
    createCanvas(windowWidth, windowHeight)
}

function draw() {
    background(0);
    stroke(white) 
    strokeWeight(2); 
    
    //function ripple;
}

